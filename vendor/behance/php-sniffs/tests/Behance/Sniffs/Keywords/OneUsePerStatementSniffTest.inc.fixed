<?php

use A, B;

class Foo {
  use F;
  use Z;
  use Y;
  use X;
  use W;
  use V;
  use G;
  use U;
  use K;
  use A, B {
    A::blah insteadof B;
  }
}

$g = function ( $h ) use ( $i ) {
  echo "not used";
};
