<?php
if( true ) {
  // whitespace between if & (
}

elseif (true ) {
  // whitespace between ( & true
}

else{
  // whitespace between true & )
}

while ( true ){
  // whitespace between ) & {
}

foreach([] as $empty){
  // all errors for foreach
}

do{
  // same here for do ... while
}while(true);

try{
}
catch( Exception $e ){
}
